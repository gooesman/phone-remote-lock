# phone-remote-lock · 手机遥控器

在电脑上控制 **小米 15 Pro**（或任意 Android 手机）：锁屏、音量、亮度、刷视频上下翻，
还有一个**全黑半透明的鼠标映射层**——用电脑鼠标直接当手机触摸屏用。

不装手机端 App、不需要 root、不需要数据线。

---

## 一、原理

全部走 **adb 无线调试**，电脑发指令、手机执行系统级动作：

| 功能 | 实现 |
|---|---|
| 锁屏 | `input keyevent 223`（KEYCODE_SLEEP，立即休眠 → 按系统设置进入锁屏） |
| 音量 ±、静音 | `input keyevent 24 / 25 / 164` |
| 播放暂停 | `input keyevent 85` |
| 亮度 步进 | `input keyevent 221 / 220`（走系统亮度键通路，能穿透应用窗口覆盖） |
| 亮度 绝对值 | `settings put system screen_brightness <0-255>`（持久化，重启后保留） |
| 刷视频 / 触摸 | `input swipe` / `input tap`，坐标按当前旋转后的分辨率换算 |
| 返回 / 桌面 | `input keyevent 4 / 3` |

---

## 二、手机端一次性设置（小米 15 Pro / HyperOS）

### 1. 打开开发者选项
设置 → 我的设备 → 全部参数与信息 → 连点 **「OS 版本」7 次**。

### 2. 打开两个调试开关（**这一步最关键**）
设置 → 更多设置 → 开发者选项：

- ✅ **USB 调试**
- ✅ **USB 调试（安全设置）** ← 不开这项，`input` 命令会**静默失效**：命令返回成功，但手机毫无反应

> 「USB 调试（安全设置）」需要**插着 SIM 卡 + 登录小米账号**才能打开。
> 建议顺手打开 **「停用 adb 授权超时」**，否则过几天要重新授权。

### 3. 打开无线调试
设置 → 更多设置 → 开发者选项 → **无线调试** → 打开 → 点进「无线调试」页 → **使用配对码配对设备**。
会看到 **IP:配对端口** + **6 位配对码**（配对用，一次性）；
配对完后同一页还有 **IP:调试端口**（连接用，**端口号和配对端口不一样**）。

### 4. 保证息屏即锁屏
设置 → 锁屏 → **自动锁定 → 立即**。

---

## 三、电脑端安装

```
双击 setup.bat      # 建虚拟环境 + 装依赖（只跑一次，含 PySide6 约 150MB）
```

启动方式：

| 方式 | 说明 |
|---|---|
| **start.bat** | 常驻托盘 + 全局热键（推荐） |
| **overlay.bat** | 全黑半透明鼠标映射层 |
| **remote.bat** | 数字菜单遥控面板 |

### 首次配对

**先把手机上的端口查出来**（别抄手机上那串，无线调试的端口每次开关都会变）：

```
.venv\Scripts\python.exe lock_phone.py discover
```

输出会分成两行，注意**配对端口和连接端口是两个不同的端口**：

```
  [连接] adb connect 192.168.31.152:42007
  [配对] adb pair 192.168.31.152:44667 <6位配对码>
```

`[配对]` 只在手机上打开「使用配对码配对设备」弹窗时才会出现（弹窗一关端口就失效）。

然后：

```
.venv\Scripts\python.exe lock_phone.py pair 192.168.31.152:44667 771748
```

配对成功后工具会记住设备，之后自动连接，并用 mDNS 自动跟踪端口变化。

### 换了 Wi-Fi / 换了网段之后

手机 IP 变了不用改配置——直接：

```
.venv\Scripts\python.exe lock_phone.py status
```

它会先试旧地址，失败就用 mDNS 重新发现，成功后**自动把新地址写回 config.json**。
如果手机完全找不到，会提示你去手机上重开「无线调试」。

也可以手动指定新地址：

```
D:\adb\platform-tools\adb.exe connect 192.168.31.152:42007
```

或直接改 `config.json` 里的 `device` 字段。

---

## 四、托盘图标：主入口

托盘图标（`start.bat` 启动后出现在任务栏右下角）是主要入口：

| 操作 | 效果 |
|---|---|
| **左键单击** | **打开控制面板 + 映射层**（默认；可在 config 的 `left_click` 里改成只开面板 / 只开映射层 / 不响应） |
| 右键单击 | 完整菜单（面板、锁屏、映射层开关、音量、亮度、刷视频、导航、重连、退出） |

托盘程序带**单实例锁**：重复运行不会出现两个图标（避免热键被触发两次）。
要重启就先在菜单里点「退出」，再运行 `start.bat`。

## 五、控制面板

一个小窗口，按钮直接作用于手机，不用记快捷键：

- **亮度**：减 / 加 / 10% / 50% / 100%
- **音量**：减小 / 增大 / 静音
- **播放 / 刷视频**：上一个 / 播放暂停 / 下一个
- **屏幕**：锁屏 / 唤醒 / 通知栏
- **导航**：返回 / 桌面 / 任务切换
- **映射层**：开启 / 关闭（按钮会显示当前状态）
- **刷新状态 / 重新连接**

面板顶部会实时显示设备连接状态和当前亮度；如果检测到有 App 占用窗口级亮度覆盖，会直接标出来。

面板由 `panel.py` 承载，是**单实例**的（用 `127.0.0.1:59731` 做锁），
重复唤起只会把命令转给已经在跑的那个，不会多开。
关掉面板窗口只是隐藏，托盘图标可以再唤起。

## 六、鼠标映射层（全黑半透明）

全屏的**黑色半透明**窗口，中间那块竖向矩形 = 手机屏幕的等比例映射区。
鼠标只有落在这个矩形里才会作用到手机，矩形外只是背景。

| 操作 | 效果 |
|---|---|
| **左键单击** | tap 点按 |
| **左键拖动** | swipe 滑动（刷视频 / 翻页） |
| **滚轮向下 / 向上** | 下一个 / 上一个视频 |
| **右键** | 返回键 |
| **中键** | 回到桌面 |
| **↑ / ↓** | 音量 + / − |
| **← / →** | 亮度 + / − |
| **空格** | 播放 / 暂停 |
| **L / W** | 锁屏 / 唤醒 |
| **N** | 通知栏 |
| **S** | 刷新状态（含亮度覆盖提示） |
| **R** | 重新读取分辨率（手机转过屏后用） |
| **Esc** | 关闭映射层 |

透明度在 `config.json` 的 `overlay_alpha` 里调（0.05 近乎透明，1.0 全黑不透明）。
不想要映射层时，用 `left_click: "panel"` 只留控制面板，或在托盘菜单里点「映射层 → 关闭」。

> **注意**：这不是屏幕镜像，看不到手机画面，是盲操作。
> 想要「看得见手机屏幕 + 实时鼠标控制」，请用 **scrcpy**（另一套工具，效果更好但需要单独安装）。

## 七、热键与命令行

### 全局热键（默认）

| 快捷键 | 动作 |
|---|---|
| `Ctrl + Alt + L` | **锁定手机** |
| `Ctrl + Alt + P` | 控制面板 |
| `Ctrl + Alt + O` | 映射层开关 |
| `Ctrl + Alt + ↑ / ↓` | 音量 + / − |
| `Ctrl + Alt + M` | 静音 |
| `Ctrl + Alt + → / ←` | 亮度 + / − |
| `Ctrl + Alt + PageDown` | 下一个视频（上滑） |
| `Ctrl + Alt + PageUp` | 上一个视频（下滑） |
| `Ctrl + Alt + Space` | 播放 / 暂停 |

在 `config.json` 的 `actions` 里随便改。

### 命令行

```bat
lock_phone.py panel             打开控制面板
lock_phone.py overlay-toggle    切换映射层
lock_phone.py lock              锁屏
lock_phone.py vol-up            音量 +
lock_phone.py bright-up         亮度 + （按系统亮度键的步长）
lock_phone.py bright 60%        亮度设为 60%（绝对设置，持久化）
lock_phone.py bright-get        查看当前亮度 + 是否有应用占用亮度覆盖
lock_phone.py next / prev       下一个 / 上一个视频
lock_phone.py status            连接 / 屏幕 / 亮度 / 分辨率
lock_phone.py swipe 540 2200 540 700 160
lock_phone.py key 26            发任意 keycode
```

### 开机自启

`Win + R` → `shell:startup` → 把 `start.bat` 的快捷方式丢进去。

---

## 八、亮度为什么"改了没反应"

Android 13 之后的亮度是**三层叠加**的，从下往上优先级递增：

| 层级 | 设置方式 | 特点 |
|---|---|---|
| 1. 用户亮度设置 | `settings put system screen_brightness 0-255` | 持久化，重启保留 |
| 2. 系统级覆盖 | `cmd display set-brightness 0.0-1.0` | 临时覆盖，可能钉住到重启 |
| 3. **应用窗口级覆盖** | 抖音/B站**播放器的亮度手势** | **优先级最高，压住前两层** |

所以：**如果你在 B站/抖音 播放器里上下滑动过左侧屏幕，那就会设一个窗口级亮度覆盖**，
此时不管从电脑发什么亮度命令，都看不到变化（本工具会检测到并提示是哪个 App 在占用）。

- **步进调节（←→ / 热键）**走的是系统亮度键通路，实测**能穿透**第 3 层覆盖；
- **绝对设置（`bright 50%`）**走第 1 层，在播放器覆盖生效时可能看不出变化。

排查命令：`lock_phone.py bright-get`，会一并告出占用覆盖的包名。

实测数据（小米 15 Pro / HyperOS / Android 16）：
- 生效亮度 = `设置值 / 255 × 0.25`（0.25 是这块屏的常规亮度上限，再往上进 HBM 高亮模式）
- 一次亮度键事件 ≈ 设置值 ±12

---

## 九、配置说明（config.json）

| 键 | 默认 | 说明 |
|---|---|---|
| `adb_path` | `D:\adb\platform-tools\adb.exe` | 留空则自动在 PATH 和常见目录里找 |
| `device` | 空 | 连接成功后自动写入 |
| `actions` | 见上表 | 热键 → 动作 映射 |
| `left_click` | `both` | 托盘左键行为：`both` 面板+映射层 / `panel` 只开面板 / `overlay` 只开映射层 / `none` 不响应 |
| `panel_port` | 59731 | 控制面板的单实例锁端口（改了就两边都改） |
| `panel_poll_seconds` | 8 | 面板可见时刷新状态（亮度等）的间隔；设 0 关闭刷新 |
| `brightness_disable_auto` | true | 绝对设置亮度前先关掉自动亮度，否则会被系统覆盖 |
| `brightness_key_repeat` | 1 | 每次亮度步进发几次亮度键（调大步长用） |
| `warn_window_override` | true | 检测到应用占用亮度覆盖时给出提示 |
| `swipe_duration` | 160 | 上/下滑动的毫秒数。滑不动就加大到 250-300 |
| `overlay_alpha` | 0.55 | 鼠标映射层的黑色不透明度 |
| `overlay_margin` | 0.06 | 映射区上下留白比例 |
| `auto_discover` | true | 用 mDNS 自动发现无线调试设备（端口变了能自愈） |
| `allow_kill_server` | true | 重连失败时允许重启本地 adb server 再试一次 |
| `retry_cooldown` | 4.0 | 连接失败后多少秒内不再跑完整重连（避免按钮卡住） |
| `notify` | true | 动作完成后弹托盘气泡 |

## 十、常见问题

**Q：命令返回 `OK`，但手机完全没反应**
A：`USB 调试（安全设置）` 没开。小米特有开关，不开的话 `input` 系列命令全部静默失效。

**Q：手机息屏了但没上锁**
A：设置 → 锁屏 → 自动锁定 → **立即**。

**Q：亮度调了没反应**
A：先跑 `lock_phone.py bright-get`。如果提示某个 App 占用窗口亮度覆盖，那就是抖音/B站播放器的亮度手势压住了；
在播放器左侧把亮度手势滑回最大值，或退出播放器即可。步进调节（←→）不受这个影响。

**Q：鼠标映射层的点击位置不准**
A：手机旋转后按一下 `R` 重新读取分辨率。映射是按当前 `cur=` 尺寸等比换算的，横竖屏都能用。

**Q：映射层里点了但没反应 / 反应慢**
A：每次手势都是一次 adb 调用，延迟约 100-300ms，属于正常。快速滚动时会排队列。
这不是实时镜像，要实时请看 scrcpy。

**Q：连接失败 / 过一会儿就断**
A：① 手机没开「无线调试」；② 不在同一 Wi-Fi；③ 端口变了。
跑 `lock_phone.py discover`，或托盘菜单点「重新连接」（会走 mDNS 自动发现）。

**Q：映射层报缺少 PySide6**
A：重跑 `setup.bat`，或 `.venv\Scripts\python.exe -m pip install PySide6`。
（托管 Python 3.13 不带 tkinter，所以这里用 Qt 实现。）

**Q：热键按了没反应**
A：检查 `Ctrl+Alt+方向键` 是否被 Intel 显卡的旋转热键占用；看 `lock_phone.log`。

**Q：托盘出现了两个图标 / 热键按一次触发两次**
A：旧版本没有单实例锁。双击 **`restart.bat`** —— 它会清掉所有旧的托盘/面板/映射层进程再重新启动一个。
新版本已经带单实例锁，重复双击 `start.bat` 不会再多开。

**Q：提示"无线调试连接已失效（offline）"**
A：说明手机端 adb 的 TLS 会话僵死了（网络本身可能是通的）。处理顺序：
1. 手机上：开发者选项 → **无线调试** → 关掉再打开（端口会变，本工具会自动重新发现）
2. 还不行就「取消配对」后用新的配对码重新 `pair` 一次
3. 本工具已经会自动做：断开重连 → mDNS 重新发现 → 重启本地 adb server（各一轮）

**Q：按钮点了要等一会儿才生效**
A：手机离线时每次操作都要走重连流程。工具加了失败冷却（`retry_cooldown`，默认 4 秒），
所以离线期间按钮会快速返回错误而不是卡住。手机在线时是 100-300ms 级别。

## 十一、备选方案：不想要 adb 的话

手机装 **MacroDroid**：
1. 新建宏：触发器 = **HTTP Server Request**（或 Webhook）
2. 动作 = **锁屏**
3. 电脑上 `curl http://<手机IP>:<端口>/lock` 触发

优点：重启手机不用重连、不依赖 adb。缺点：要装 App、要给辅助功能/设备管理员权限。

---

## 文件清单

```
lock_phone.py        托盘 + 热键 + 命令行 + adb 通信层（主程序）
panel.py             控制面板 + 映射层宿主（Qt，单实例）
overlay.py           全黑半透明鼠标映射层（也可单独运行）
config.example.json  配置模板（参考用；缺 config.json 时会自动用内置默认值）
config.json          你的实际配置（含设备地址，已被 .gitignore 排除）
requirements.txt     依赖列表
setup.bat            一次性安装
start.bat            启动托盘（常驻）
restart.bat          清掉旧进程并重启托盘（升级后用这个）
panel.bat            单独打开控制面板
overlay.bat          单独打开映射层
remote.bat           数字菜单遥控面板（纯命令行版）
lock.bat             锁屏一次（带输出）
lock_silent.vbs      静默锁屏（可绑定快捷方式热键）
lock_phone.log       运行日志（已被 .gitignore 排除）
test-logs/           开发期的诊断输出，可整个删掉
```
